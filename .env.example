ANTHROPIC_API_KEY=
TELEGRAM_BOT_TOKEN=
TXLINE_NETWORK=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
TXLINE_API_ORIGIN=https://txline-dev.txodds.com
# Base58 secret key of the service wallet that signs the one subscribe tx (devnet only).
SERVICE_WALLET_SECRET=
# Cached API token from a completed activation (set after `npm run activate`).
TXLINE_API_TOKEN=
LOG_LEVEL=info
DATABASE_PATH=data/pitchwire.db
